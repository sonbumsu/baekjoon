#include <iostream>
#include <string>
using namespace std;
int main() {
    string a, b;
    cin >> a >> b;
    if (a == b) {
        cout << 0 << endl;
    } 
    else {
        cout << 1550 << endl;
    }
    return 0;
}